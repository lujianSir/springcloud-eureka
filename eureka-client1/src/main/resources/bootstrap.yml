spring:
  application:
    name: spring-cloud-client1 #服务消费者
  cloud:
    config:
      name: config-single-client #需要从github上读取的资源名称，注意没有yml后缀名
      profile: prod   #本次访问的配置项
      label: master
      uri: http://localhost:6002/





